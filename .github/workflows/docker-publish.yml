name: Build and Publish Tdarr Node Image

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify GPU startup gate
        run: bash tests/entrypoint-test.sh

      - name: Verify NVENC device filter
        run: |
          gcc -std=c11 -Wall -Wextra -DNVENC_FIX_TEST nvenc-device-filter.c -o /tmp/nvenc-filter-test
          /tmp/nvenc-filter-test
          gcc -std=c11 -Wall -Wextra tests/nvenc-ioctl-forwarding-test.c -o /tmp/nvenc-ioctl-forwarding-test
          /tmp/nvenc-ioctl-forwarding-test
          gcc -shared -fPIC -O2 -Wall -Wextra -o /tmp/nvenc-device-filter.so nvenc-device-filter.c -ldl

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ghcr.io/bl4ut0/tdarr-node-runpod:2.85.01
            ghcr.io/bl4ut0/tdarr-node-runpod:latest
